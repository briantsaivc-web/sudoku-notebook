# 數獨筆記本 Sudoku Notebook — 規格書

版本:1.0 · 最後更新:2026-09-13

## 1. 專案目標
一個純前端、免安裝、免登入的網頁版數獨遊戲。單人模式(沒有真正的對手,是玩家 vs. 題目本身),
在 PC、筆電、iPad、手機上都能快速上手,操作極簡。可離線遊玩,可加到手機主畫面像 App 一樣使用。

## 2. 數獨規則
9×9 盤面,分成 9 個 3×3 宮。每一列、每一行、每一宮中數字 1–9 都恰好出現一次。
合格題目必須「唯一解」——產生題目時會強制檢查,不合格會直接丟棄重新產生。

## 3. 難度設計
不以「挖空幾格」作為難度依據,而是以**解題所需的技巧等級**分級,理由是純看給定格數的
分級誤差很大(同樣 30 格的題目,難度可能天差地遠)。

分級用的技巧集合(定義於 `tools/generate-puzzles.js` 的 `gradeHumanSolve`):

| 等級 | 技巧 | 說明 |
|---|---|---|
| 1 級(簡單) | 單一候選數(naked single)、宮/列/行唯一候選位置(hidden single) | 最基礎的排除法 |
| 2 級(中等) | 1 級 + 區塊限定(locked candidates:pointing / box-line reduction)、數對排除(naked/hidden pair) | 需要看兩個以上格子的關聯 |
| 3 級(困難) | 2 級 + 三數對排除(naked/hidden triple)、X-Wing | 需要跨列/行的模式辨識 |

生成流程:
1. 隨機回溯法產生一個合法填滿的 9×9 盤面。
2. 隨機挖空格,每挖一格都用「解答計數器」(最多算到 2 組解)驗證仍為唯一解,否則放回。
3. 挖到目標格數後,用上面的技巧分級器實際「解一次」,能解完所需的最高技巧等級 = 這題的難度。
4. 若技巧分級器解不完(代表需要用猜的/試誤法),直接丟棄,不放進題庫。
5. 依分級結果放入對應難度的題庫,直到 3 個難度各湊滿 100 題。

給定格數大致範圍(僅供參考,實際難度以技巧分級為準):
- 簡單:約 38–45 格
- 中等:約 30–37 格
- 困難:約 24–29 格

## 4. 題庫規模:每級 100 題
- 100 題是常見的實體數獨書份量,玩家有「破關進度感」(例如「困難 47/100」),比無限生成
  更有成就感,也方便做「已完成題數」的統計畫面。
- 3 個難度 × 100 題,離線先生成、驗證好難度,壓成精簡字串後整個題庫檔案約 50KB,
  完全不影響網頁載入速度,也不會在手機上即時運算卡頓。
- 每個難度內的題目依「給定格數」由多到少排序,所以第 1 關通常比第 100 關略難一點,
  難度曲線平滑漸進。
- 之後想擴充題量,重新調整 `tools/generate-puzzles.js` 裡的 `NEED` 數字重新跑一次即可,
  不需要更動任何架構。

## 5. 功能規格

### 5.1 筆記(鉛筆標記)
- 切換「筆記模式」後點空格,格子內會用 3×3 小格排版顯示候選數字(1 在左上、5 在正中央…),
  對應紙上鉛筆打小字的直覺動作。
- 再點一次同一個數字會取消該筆記。
- 一旦該格填入正式答案,筆記自動清空。
- 一般填答與筆記字用不同顏色區分(見「外觀主題」章節的 `--text-user` / `--text-note`)。

### 5.2 提示
- 每局 3 次,用完後提示鍵反灰不可再按。
- 優先對「目前選取且是空格」的格子給提示;若沒有選取空格,對盤面上第一個空格給提示。
- 提示直接寫入正解數字並清除該格筆記,計入使用次數,但**不會**因為之後「復原」而歸還次數
  (你可以復原掉提示填的數字,但那次提示額度不會退還——避免無限復原刷提示)。

### 5.3 計時
- 進入題目自動開始計時,完成時自動停止並定格顯示。
- 切換分頁 / 離開頁面(`visibilitychange`)會自動暫停,回來後蓋一層「▶ 繼續遊戲」遮罩,
  避免離開時被偷看或誤觸。
- 點擊計時器本身也可手動暫停/繼續。
- 每題記錄「最佳時間」,破紀錄時完成畫面會特別標示「新紀錄!」。

### 5.4 關卡進度
- 首頁依難度顯示 1–100 的選題面板,已完成打勾並顯示最佳時間,方便掌握「目前卡在第幾關」。
- 進度存在瀏覽器 `localStorage`,不需要帳號,但**不會**跨裝置同步(換手機/清瀏覽器資料
  會重置)。如果之後需要跨裝置同步,需要加上帳號系統 + 後端資料庫,是另一個規模的專案。
- 未完成的題目離開時會自動存檔(每 5 秒 + 每次操作後存一次),回首頁會看到「繼續上次的題目」
  提示,一次只保留最近一個進行中的題目。

### 5.5 其他操作細節
- 支援滑鼠/觸控點擊,也支援桌機鍵盤:數字鍵 1–9 填數、Backspace/Delete/0 清除、方向鍵移動
  選取格、`N` 鍵切換筆記模式。
- 點選任一格會高亮同列/同行/同宮(淺色)與盤面上數值相同的格子(較深色),方便肉眼掃描。
- 數字衝突(同列/行/宮重複)即時標紅,不阻擋輸入(玩家可以先填錯再自己發現修正,貼近紙上
  遊玩的體感,不是強制卡關)。
- 復原(undo)會記錄每一步的填答與筆記變更,可連續復原到題目剛開始的狀態。

## 6. 外觀主題
四種可即時切換的視覺主題(切換即時生效,存在 `localStorage`,預設「紙感筆記」):

| 主題 | 說明 |
|---|---|
| 經典簡約 classic | 白底深藍字,線條俐落 |
| 紙感筆記 paper(預設) | 米黃紙感背景、鉛筆感填字色,虛線宮格 |
| 深色極簡 dark | 深色背景 + 薄荷綠強調色,適合夜間 |
| 柔和圓角 soft | 暖色系、大圓角、珊瑚橘強調色 |

實作方式:所有顏色都是 CSS 變數(定義在 `css/styles.css` 的 `:root[data-theme="..."]`),
切換只是換 `<html data-theme="...">` 屬性,不需要重新渲染 DOM 結構。

## 7. 響應式 / 跨裝置設計
- 純 CSS Grid + `aspect-ratio` 讓 9×9 盤面在任何寬度下都維持正方形。
- 寬度 ≥ 681px(桌機、筆電、iPad 橫向):盤面在左、數字鍵與工具列在右側。
- 寬度 ≤ 680px(手機、iPad 直向的邊界情況):自動改為上下堆疊,數字鍵排成一整排在下方,
  觸控熱區加大。
- 字級用 `clamp()` 隨容器寬度縮放,盤面數字在小螢幕也維持可讀性。
- 已測試桌機(1024px 寬)、手機(375px 寬,iPhone 尺寸)兩種寬度下的排版與互動。

## 8. 技術架構
- 純靜態網站:HTML + CSS + Vanilla JS,不需要建置工具(no bundler/no framework),
  可以直接丟上任何靜態網頁空間(GitHub Pages、Netlify、隨便一台網頁伺服器都行)。
- 題庫(含正解)在建置期離線產生一次(`node tools/generate-puzzles.js`),瀏覽器端**不需要**
  跑求解器或產生器,大幅減少程式碼量與執行負擔——瀏覽器只需要基本的「同列/行/宮是否重複」
  檢查,不需要完整的數獨求解演算法。
- PWA-lite:`manifest.webmanifest` + `sw.js` 提供「加到主畫面」與基本離線快取(開過一次
  之後斷網也能繼續玩,因為整個題庫都已經在本地)。

### 檔案結構
```
sudoku-notebook/
├─ index.html              # 唯一的頁面,首頁與遊戲畫面都在同一個檔案裡切換
├─ css/styles.css          # 版面 + 四種主題的 CSS 變數
├─ js/app.js               # 遊戲邏輯與 UI 控制(純前端,無相依套件)
├─ data/puzzles.js          # 自動產生的題庫(300 題,含正解),不要手動編輯
├─ tools/generate-puzzles.js # 離線題庫產生器 + 技巧分級器(用 node 執行)
├─ manifest.webmanifest    # PWA 設定
├─ sw.js                   # 離線快取
├─ icons/icon.svg          # App 圖示
└─ SPEC.md / README.md
```

### 本機資料儲存(localStorage)key
- `sn:progress` — 三個難度各 100 題的完成狀態與最佳時間
- `sn:current` — 目前進行中的那一題(續玩用),完成後會清除
- `sn:theme` — 目前選擇的外觀主題
- `sn:lastLevel` — 首頁上次選取的難度分頁

## 9. 已知限制 / 未來可以再加的東西
- 進度只存在單一瀏覽器裡,換裝置或清瀏覽器資料會重置(如果要跨裝置同步需要帳號系統)。
- PWA 圖示目前只有 SVG,iOS「加到主畫面」的圖示可能不是精緻的點陣圖(可以之後再補
  `icons/icon-192.png`、`icons/icon-512.png`)。
- 難度分級技巧集合是常見的中階子集(不含 swordfish、XY-Wing 等更進階技巧),困難題目
  對重度玩家來說可能仍偏中等——如果覺得「困難」不夠難,可以在 `tools/generate-puzzles.js`
  加入更多技巧再重新產生題庫。

## 10. 部署
靜態網站,部署在 GitHub Pages:專案根目錄直接發布(`main` 分支 `/ (root)`)。
本機開發測試可用任何靜態伺服器,例如:
```bash
python -m http.server 5173
```
